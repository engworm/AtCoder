#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <functional>
#include <deque>
#include <cmath>
#include <numeric>
#include <deque>
#include <stack>
#include <set>

class carpet {
  public:
  carpet(int n) : cp(std::vector<std::vector<char>> (n, std::vector<char>(n, 0))) {};
  private:
    std::vector<std::vector<char>> cp;
};

int main() {
  return 0;
}